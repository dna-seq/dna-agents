---
description: Curate published PGS Catalog entries into a PGS module.
---

Delegate this request to `pgs-module-creator`. It must use real `PGSxxxxxx`
identifiers, record training ancestry, retain research-use caveats, and never
author per-variant PGS weights. Validate the resulting `pgs.csv` module using
`validate_spec`.
